// Preload runs in an isolated context with access to Node + the DOM.
// Nothing is exposed yet — all persistence happens in the renderer via
// IndexedDB. Reserved for future IPC (e.g. native file dialogs) if needed.
